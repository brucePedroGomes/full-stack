# Local development only. Copy to .env and use uv run --env-file .env.
DJANGO_DEBUG=true
DJANGO_SECRET_KEY=django-insecure-local-development-only
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,[::1]
DB_NAME=challenge
DB_USER=challenge
DB_PASSWORD=local-dev-password
DB_HOST=127.0.0.1
DB_PORT=5439
DB_SSLMODE=disable
